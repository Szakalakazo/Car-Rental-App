/**
   * Created by Tomek on 29.08.2019.
   */
  ({

      navigate: function(component, event, helper) {
              let urlValue =  component.get("v.currentImageURL");
              console.log('urlValue ' + urlValue);
          }
  })